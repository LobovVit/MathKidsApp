# MathKidsApp full v6

Полный архив исходников.

Что исправлено:
- `import Combine` добавлен в сервисы/роутер/VM, где есть `ObservableObject`
- `HomeView` адаптирован для iPhone portrait
- карточки меню компактнее на узких экранах
- навигация одноконная на macOS
- `ContentView` использует `AnyView` для лучшей совместимости со SwiftUI/macOS 11

PIN по умолчанию: `0000`
